// Integration test placeholder for the client.
describe("CacheLabClient", () => {
  it.todo("returns cached results when embeddings are similar");
  it.todo("falls back to origin on cache miss");
});