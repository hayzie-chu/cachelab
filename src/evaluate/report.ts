// Replay report formatter scaffold.
import type { ReplayResult } from "./runner";

export function formatReplayReport<TValue>(results: ReadonlyArray<ReplayResult<TValue>>): string {
	void results;
	throw new Error("formatReplayReport is not implemented yet.");
}
