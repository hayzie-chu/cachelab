# Contributing

## Getting started

1. Install dependencies.
2. Run `npm test`.
3. Run `npm run lint`.
4. Open a PR with a focused change.

## Notes

- Keep source exports in `src/index.ts`.
- Add tests for new behavior in `tests/`.
